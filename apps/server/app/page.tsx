/**
 * The server app root page is a minimal health-style landing surface for local verification.
 * Real product UI lives in the dashboard package, not in this backend-focused app.
 */
export default function HomePage() {
  return (
    <main>
      <h1>Tracer Server</h1>
      <p>tRPC and analysis services are hosted here.</p>
    </main>
  )
}
