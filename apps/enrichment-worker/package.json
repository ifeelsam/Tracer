{
  "name": "@tracerlabs/enrichment-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "lint": "biome check src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracerlabs/db": "workspace:*",
    "@tracerlabs/shared": "workspace:*",
    "@upstash/redis": "^1.34.9",
    "fastify": "^5.2.1",
    "tsx": "^4.19.2",
    "viem": "^2.21.57",
    "zod": "^3.24.1"
  }
}
