/**
 * The anchor worker entrypoint will poll Redis and commit trace Merkle roots on-chain.
 * The loop implementation lands next; this file exists now so the package validates in isolation.
 */
function start() {
  return null
}

start()
